[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "COFFEEMATCH"
version = "0.0.1"
dependencies = [
    "streamlit",
    "emoji",
    "pandas",
    "numpy",
]
